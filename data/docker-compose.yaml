version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: mysql_auth_service
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: auth_service
    ports:
      - "3306:3306"